# New User Onboarding

## Goal

A newly scaffolded project should give a user confidence that the selected modules are wired correctly within a minute.

## Default Experience

- the home page renders as a small validation fixture
- the generated apps folder matches the requested composition
- the shared workspace packages are already usable
- the next implementation step is obvious
