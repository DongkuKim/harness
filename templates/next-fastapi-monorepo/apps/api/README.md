# API

FastAPI app for the monorepo.

## Commands

```bash
uv run fastapi dev app/main.py
uv run fastapi run app/main.py
uv run ruff check .
uv run ruff format .
uv run basedpyright
uv run deptry .
uv run lint-imports
uv run pytest -n auto
uv run pip-audit
```
